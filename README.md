# Candidate Ranker UI (React)

This single-page app ranks CSV-exported candidates by keyword groups. The interface is built with React and runs entirely in the browser. To use:

1. Open `index.html` in a browser.
2. Upload a CSV of candidates.
3. Adjust keyword groups and weights.
4. Rank and export the top candidates.

Run tests with:
```bash
npm test
```
