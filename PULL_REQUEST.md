# Pull Request

## Title
external-js-dynamic-config

## Description
Move all JavaScript logic from index.html into an external app.js file, clean up HTML, and add a dynamic/configurable keyword group modal with localStorage persistence. UI is now more maintainable and user-friendly.